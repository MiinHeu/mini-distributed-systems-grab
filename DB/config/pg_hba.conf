# TYPE  DATABASE        USER            ADDRESS             METHOD

# Trust all connections - development only
local   all             all                                 trust
host    all             all             0.0.0.0/0           trust
host    all             all             ::/0                trust
host    replication     replicator      0.0.0.0/0           trust
